# HPAT v6 — Python dependencies
# Core (required)
# tkinter is bundled with Python standard library

# Market data feed — without these, runs in SIM mode
aiohttp>=3.9.0          # non-blocking REST API calls
websockets>=12.0        # async WebSocket client

# Analytics
numpy>=1.26.0           # vectorised RSI/ATR calculations

# Financial precision (Python stdlib — no install needed)
# decimal — bundled

# Security: credential management
python-dotenv>=1.0.0    # load API keys from .env file

# ─── AI Provider SDKs (install the ones you use) ──────────────────────────

# GEMINI (Google) — https://aistudio.google.com/apikey
google-genai>=1.0.0         # New recommended SDK (preferred)
# google-generativeai>=0.8.0  # Old SDK (auto-fallback if google-genai missing)

# GROQ — https://console.groq.com/keys
groq>=1.0.0                 # Groq Cloud ultra-fast inference

# OPENROUTER — https://openrouter.ai/keys
openai>=1.0.0               # OpenRouter uses OpenAI-compatible API
